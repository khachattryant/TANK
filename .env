NODE_ENV=development
PORT=4001

# MongoDB Atlas connection string
MONGODB_URI="mongodb+srv://tk29500:Tatevik2002@cluster0.ingtpgf.mongodb.net/"
FIRST_ADMIN_EMAIL="admin@example.com"
FIRST_ADMIN_PASSWORD="StrongPassword123!"
# Gmail SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=tk29500@gmail.com
SMTP_PASS=ylmwcptqweangzmq
SMTP_FROM="tank@gmail.com"

NEXT_PUBLIC_BACKEND_BASE_URL=http://localhost:4001

PUBLIC_BASE_URL="http://localhost:3000"


# JWT signing secret (must be long/random)
JWT_SECRET="change-me-to-a-long-random-secret"

# Seed admin user (used if no admin exists yet)
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="your-strong-password"

# Local filesystem folder where uploaded images go
UPLOADS_DIR="uploads"

# Used to build absolute image URLs returned by API
# PUBLIC_BASE_URL="http://localhost:4001"