{
  "name": "tank-am-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "lint": "echo \"Add backend linting if desired\"",
    "bootstrap:first-admin": "tsx scripts/bootstrap-first-admin.ts"
  },
  "dependencies": {
    "@fastify/cors": "*",
    "@fastify/helmet": "*",
    "@fastify/multipart": "*",
    "@fastify/swagger": "*",
    "@fastify/swagger-ui": "*",
    "bcrypt": "*",
    "dotenv": "*",
    "fastify": "*",
    "jsonwebtoken": "*",
    "mongoose": "*",
    "nodemailer": "*",
    "zod": "*"
  },
  "devDependencies": {
    "@types/bcrypt": "*",
    "@types/jsonwebtoken": "*",
    "@types/mongoose": "*",
    "@types/node": "*",
    "@types/nodemailer": "^7.0.11",
    "tsx": "*",
    "typescript": "*"
  }
}
